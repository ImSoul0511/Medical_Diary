---
trigger: manual
--- 
# This task is about creating mock data for Medical Diary Project
- For modules like update heart rate, steps count,... currently there's no way to test them, as well as to update them. The plan was that we're gonna create a app called Health Connect to simulate the data from smart watch or other health devices and send it to our app. Time is not supporting us so it's gonna be postponed for now.
- We need to create mock data for these modules to test them.
- The mock data should be created in the `backend/mock_data` directory.
- The mock data should be in the format of JSON files.
- Create 3 accounts: 1 user, 1 doctor, 1 admin.
- For each account, create mock data for all modules.
