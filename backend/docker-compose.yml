version: '3.8'

services:
  api:
    build: .
    container_name: medical_diary_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app:/code/app
    restart: unless-stopped
