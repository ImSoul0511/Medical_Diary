services:
  api:
    build: .
    container_name: medical_diary_backend
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    env_file:
      - .env
    dns:
      - 8.8.8.8
      - 1.1.1.1
    restart: always
